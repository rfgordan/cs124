import java.util.PriorityQueue;

/**
 * NaiveHeap.java
 */

public class NaiveHeap<T> implements Heap<T> {

    /* ArrayList that represents the heap */
    PriorityQueue<T> queue;

    @Override
    public void insert(T obj, double val) {
        queue.add
    }
    public T deletemin();
}